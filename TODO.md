# TODO

- [ ] Explain the banner/image quality process for this Next.js project (master image size, format, and Next/Image configuration).
- [ ] Identify where banners are rendered (hero sections using `next/image` with `fill` + `object-cover`).
- [ ] Recommend a concrete export checklist (pixel size, WebP/AVIF settings, cropping-safe area).
- [ ] Optionally propose a small code improvement: add `sizes` to hero/banner `next/image` calls.
- [ ] Provide final answer describing the process end-to-end.

